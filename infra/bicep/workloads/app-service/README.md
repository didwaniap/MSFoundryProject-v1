# App Service Workload Template

Resource-group-scope template for agentic apps hosted on Azure App Service.

It creates:

- User-assigned managed identity.
- Linux Basic B1 App Service plan.
- Linux container Web App.

This template can incur standing App Service Plan charges. Keep it deferred until App Service quota and demo budget are confirmed.
